export interface LoginResponse {
  data: {
    access_token: string;
    // Add other properties if needed
  };
  // Add other properties if needed
}

export interface RegisterResponse {
  data: {
    access_token: string;
    // Add other properties if needed
  };
  // Add other properties if needed
}
