export class Owner {
  id: number;
  tag: string;
  name: string;
  avatar: string;

  constructor(id: number, tag: string, name: string, avatar: string) {
    this.id = id;
    this.tag = tag;
    this.name = name;
    this.avatar = avatar;
  }
}
